//single number 24
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
int n;
printf("enter the va;ue of n:");
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
     for(int i=0;i<n;i++){
         int isUnique=1;
     for(int j=0;j<n;j++){
         if(arr[i]==arr[j] && i!=j){
             isUnique=0;
             break;
         }
     }
    if(isUnique){
        printf("%d ",arr[i]);
        }
    }
    return 0;}
